# 2026-09-24

## 热点真机验收事故与硬约束升级

- 用户提供A/B两端日志：A开热点、B连热点。A准备流程把上游以太网DHCP改静态并添加实验地址，用户报告B随后无法联网；B原热点地址已合格，准备仍改静态/添加地址/改网络类别。双方GUI有渲染日志，但认证未开始；A无会话UNMET/主动停止INVALID_RUN，B discovery AddMembership 10022导致Host和四个Client场景HARNESS_ERROR。
- 用户明确软件及验收不可导致任一端断网，短暂也不接受。沿用现网；不能以UAC/风险提示/可撤销为理由改DHCP/IP/网关/DNS/路由/热点/ICS/网络类别凑验收。旧包暂停重试，不默认运行Undo；先确认当前联网状态，恢复操作另行核实和授权。
- 只读核查：VirtualAdapterFilter包含virtual，热点描述若命中会被过滤，但A实际热点完整属性待核实；CreateReceiver确实逐IP binding加组、不按接口去重，B同WLAN双IP重复加组为10022高可信假设，尚无逐次记录或受控复现实证。相同实验前缀写到A上游与B下游不证明二层互通。
- 后续修复：取消普通验收lab改网引导，窄范围支持已核实的热点下游接口，不放开全部虚拟/VPN；receiver按接口去重并保留各IP真实掩码/绑定，补诊断与回归。RFC1918/同子网/TLS/pinning/HMAC不放宽。#52受#53修复阻断，M4未通过，不能进入M5。
- 本轮仅源码/用户日志分析与HANDOFF §18.12、项目记忆、任务状态更新；没有修改产品代码、没有构建测试、没有运行网络命令或读取/应用恢复状态。记录为本地未提交修改；需先问两端联网是否已恢复。

## 用户再次纠正产品方向

- 用户明确这是原始需求而非新扩展：同局域网内安装并运行本软件的机器应自动发现，不更改任何系统网络协议配置、不转静态IP。A/B仅角色，不能绑定实验IP；软件应适应现网，发现失败先修软件或诊断阻断，不让用户改网配合。
- 已将原始需求补入HANDOFF §18.12和长期记忆。此次仅确认与记录边界，未改产品代码或系统网络；用户并未明确回复当前A/B是否均恢复联网，不能把“嗯对”记成已恢复。

## 现网修复实现与本机验证

- 已提交代码 `bc8cf48`：WFD仅按Wireless80211、正索引、系统精确描述（可带 #正整数）豁免virtual，其余VPN等拒绝词保留；receiver按一致index/Id去重，地址绑定和sender不压缩，逐接口记录加组成功/失败及错误码。
- GUI/CLI/直接旧role/elevated调用不再能改网；仓库旧PS参数后无条件throw，保留历史正文但不可执行。旧M2打包器拒绝，新M4每次独立发布目录，不删除旧目录，过滤脚本/旧手册，仅一份现网说明。
- Debug/Release各1305 PASS（Core125、Protocol299、Integration3、Security66、Transport633、Acceptance179），零失败/跳过，构建零警告/错误；Python打包/脚本静态回归10 PASS。三变异7/11/2例红→恢复同测试绿，已保存baseline/mutant及源码测试哈希；M03校验脚本把SetMulticastOption误期待为SetSocketOption，保留误报并单独复核，不改原日志。
- 只读查询确认A热点if13=Microsoft Wi-Fi Direct Virtual Adapter #2、192.168.137.1/24；以太网if6当前DHCP且无实验1.10。Release真实host运行3秒：热点入选、组播加入成功、TCP45873绑定现有137.1，正常清理无故障；无对端所以UNMET不是认证PASS。没有改网、Undo、重启网卡/热点，没有证明B恢复或双方互联网连续可用。
- 当前处于发布新候选包与记账收尾；M4仍待修复版GUI/两机认证及双方联网不受影响的人工证据，不能进入M5。

## 最终包级收口

- 首次包级旧命令拒绝退3正确，但解析失败绕过RunAsync编码初始化，重定向中文为系统代码页；修WriteUsage入口编码，提交72bfb93，再次双配置1305全绿、0警告错误。
- 最终包264项、60,242,991 bytes，SHA256=9d947a68cdd127dd73ca46f9cdbf6e7e9eec08a52c4493ed84bc33ee4caa434e；CRC/唯一扁平/逐publish一致/唯一manual/无脚本/AMD64 GUI/程序集含72bfb93标记均核验。
- 最终包EXE无窗口host实跑：现有if13热点组播与TCP45873启动、3秒后无故障收尾，无对端UNMET；旧prepare-lab+elevated真实拒绝退3，UTF-8提示正确。未运行GUI或两机认证，旧PS未执行。
- 首批现网检查前后所列接口、Preferred IPv4、DHCP、默认路由、DNS快照一致；非持续监控、非B侧证据。ADR-045覆盖旧产品改IP计划，HANDOFF§18.13及README已同步新版流程。

## 交付前记录核验

- 恢复会话后复核最终Debug/Release构建测试原始日志和Python10项结果；重新计算最终ZIP的SHA256、CRC、大小及264成员，与包级记录一致。本次只做文档收尾和产物核验，没有重跑或冒称新增测试。
- HANDOFF阶段0–4完成状态已对齐；§18.11旧包及准备A/B指引明确停用，以§18.13及ADR-045为准。包关联最后代码72bfb93，后续纯文档提交不要求重新打包。
- 交付物为现网修正版ZIP及M4-现网验收说明.txt；下一关仍须用户确认双方当前联网正常并提供修复版GUI/双机认证日志。没有修改网络或默认执行旧Undo，不进入M5。
