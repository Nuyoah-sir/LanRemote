# 2026-09-21

## M3 开工前：外部评审与实测的分工（用户问「有没有要问国外模型的」）

用户提议帮我转发 prompt 给国外模型。我的结论与分工：

- **值得问**：设计红队评审（「我漏了什么」）。已写好 Prompt A → `docs/M3_EXTERNAL_REVIEW_PROMPT.md`，
  含完整上下文（无 CA/无云/同子网/TLS1.2-1.3/指纹 pinning/1MiB framing/M3 不含认证），
  并要求模型把结论标注 VERIFIED vs RECOLLECTION。
- **不值得问**：ADR-018 的 EphemeralKeySet 风险 → 本机 spike（SslStream server+client 握手）30 分钟定案，
  且 ADR-018 本来就强制要求这条集成测试。模型的回忆不能写进 HANDOFF（严禁伪结论）。
  Prompt B 写成条件性的，只有 spike 失败并带着真实异常原文才发。
- **绝对不问**：Windows 实测行为（防火墙/网卡/改 IP/netsh 退出码）、本项目已有实测结论、
  规格里写死的硬约束（问了只会得到「放宽约束」的建议）。

## 本机环境实测（修正：我第一版读错了）

- 实测 `Win32_OperatingSystem.Caption` = **Microsoft Windows 11 专业版**，Version 10.0.26200，Build 26200，
  DisplayVersion 25H2 → 本机是 **Windows 11 25H2**，不是 Windows 10。
- **陷阱**：`HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\ProductName` 在 Windows 11 上仍是
  `Windows 10 Pro`。我第一版只读了这个值就写下「Windows 10 Pro 25H2 / build 26200」——**这个组合不成立**。
  判系统版本必须用 `Win32_OperatingSystem.Caption`，不能用这个注册表值。
  （这是外部模型纠正我的，我用 CIM 复测确认它对。）

## 外部评审对 Prompt A 的补充（已并入文件）

用户把 Prompt A 转给国外模型，回来 5 组补充（pre-auth inert / TOCTOU 快照 / 身份冲突 /
absolute deadline / strict parser）+ 一条待质疑命题 + 建议加第三标签 `NEEDS LOCAL EXPERIMENT`。
已全部并入 `docs/M3_EXTERNAL_REVIEW_PROMPT.md`，并加了三桶输出结构（A 设计必改 / B 值得测 / C 不能入 HANDOFF）。

我在文件末尾写了「⛔ 不外发的本机立场」，其中两条代码实测发现：
- `DiscoveryDeviceCache` 是 `Dictionary<Guid,...>` + `Upsert` last-write-wins → 同 deviceId 换 IP/换指纹**静默覆盖**（实测代码事实）
- 但不能一律判冲突：多网卡时「同 deviceId + 同指纹 + 不同 IP」是良性的；只有「同 deviceId + 不同指纹」可疑
- 归属：M3 只做连接目标不可变快照；改缓存语义属 M4/M9，**必须单独立 ADR，不得顺手改 M2 已验收行为**

## 红队评审回来后的分流（产出 `docs/M3_REVIEW_TRIAGE.md`）

评审 27 条，按三桶处理。**我没有直接采信它标 [VERIFIED KNOWLEDGE] 的 .NET 默认值，全部本机实测了一遍**
（临时 console 项目在 %TEMP%，反射打印 `SslXxxAuthenticationOptions` 默认值 + JSON 经验测试）：

- `AllowDuplicateProperties` 默认 **True** 且**后者覆盖前者** → 证实，且比评审说的"歧义"更严重
- `AllowTlsResume` **两端默认都是 True**（评审只点了客户端）；`EnabledSslProtocols` 默认 `None` → 评审漏了这条
- `MaxDepth` 属性值默认 **0**（内置上限 64），评审写成"默认 64"需修正
- 我第一版经验测试因属性名大小写不匹配得出假结论（看着像"没拒绝"），改对 `[JsonPropertyName]` 后才测准
  → **经验测试本身也要自检**

新增两条 ADR：
- **ADR-027** 发现层身份冲突不得静默 last-write-wins（M3 不改 M2；最晚 M4 落地冲突标记 + 禁用连接；
  同 deviceId+同指纹+不同 IP 是多网卡良性广播，不得误杀）
- **ADR-028** M3→M4 契约：上下文必须不可变携带 `{deviceId, endpoint, expectedPin, presentedPin}`，
  **M4 transcript 必须绑定 presentedPin**（只绑 expectedPin 会留下凭据中继缺口）——这条是评审最有价值的发现，
  我原以为规格的 "modified cert fingerprint fail" 已覆盖，其实那是篡改场景，不等价

未跑的实测（不得写成结论）：多网卡重叠子网、取消延迟、Win10 22H2（本机只有 Win11 25H2）。

## ADR-018 实测收口 → **ADR-029（重大：原决策是错的）**

真实 `SslStream` server/client 握手 spike（loopback，证书 profile 严格复刻 `DeviceCertificateService`，
3 flag × 3 协议 × 重复 3 次）：

- **`EphemeralKeySet` 9/9 失败**（TLS 服务端）。服务端真实异常：
  `AuthenticationException: Authentication failed because the platform does not support ephemeral keys.`
  ← `Win32Exception: 安全包中没有可用的凭证（0x8009030E = SEC_E_NO_CREDENTIALS）`；
  客户端只能看到 `IOException: unexpected EOF` —— **必须抓服务端异常才定位得到**
- `PersistKeySet` 9/9 OK，但**磁盘留持久密钥副本**（`%APPDATA%\Microsoft\Crypto\Keys` 文件数 dispose+GC 后仍 +1）
- **`Default(0)` 9/9 OK，且密钥文件 dispose/GC 后删除** → ADR-018 想避免的"第二份持久化副本"只有它满足
- 协商：TLS1.2=`TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384`，TLS1.3=`TLS_AES_256_GCM_SHA384`，
  `Tls12|Tls13`→TLS1.3；握手后真实帧收发往返成功；pin（`GetRawCertData()`+FixedTimeEquals）全 True

**污染陷阱（务必记住）**：同一进程先 `PersistKeySet` 导入过同一私钥，再 `EphemeralKeySet` 导入 → **握手成功**。
我第一次的根因探针就是被这个污染骗了（显示 OK，与矩阵矛盾），重写成"新进程 + 顺序受控 + 重复 3 次"才定案。
→ 任何平台行为结论：**新进程、顺序受控、重复多次、两端异常都抓**。

**M3 第一步三件事（顺序不能变）**：改 `ImportFlags` → `Default`；
改掉锁住旧选择的测试 `DeviceCertificateTests.ImportFlags_UsesEphemeralKeySetOnly`；
新增真实握手集成测试。

**另测到的 .NET 10 硬事实**：证书校验回调参数是 `X509Certificate`（基类），**没有 `RawData`**，
算 pin 必须用 `GetRawCertData()`（编译器会直接报错，别绕过去）。
