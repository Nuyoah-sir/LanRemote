# 2026-09-22

## M4 阶段 1 完成 —— 双档 transcript + HMAC proof 纯函数核心（提交 `35506b5`）

**feat 提交 `35506b5`**（6 files, +1113/−11）；步骤 5–8 全落地；全量 **643 PASS / 0 FAIL**。

- **产出**：
  - `src/LanRemote.Security/Auth/AuthProtocol.cs`——协议词汇表单一事实源：域串
    （`LANREMOTE-AUTH-V1` / `LANREMOTE-GRANT-V1` / `server\0`）、5 帧 type、16 字段名、
    4 尺寸（nonce/proof/token/cert 均 32B）、时限初值（认证 10s / 审批 60s，provisional）、
    权限词（view/control）+ `EncodePermission`（未定义枚举值抛异常）。
  - `src/LanRemote.Security/Auth/AuthTranscriptBuilder.cs`——纯函数核心：双档 transcript
    （ClientAuthTranscript / ServerGrantTranscript）+ `ComputeClientProof` / `ComputeServerProof`；
    输入全部强类型（Guid/Span/枚举）**不收 string**；固定长度（nonce×2/cert/key）fail fast。
  - `scripts/reference/gen-auth-golden-vectors.py`（入库）——独立 Python 参考实现（标准库），
    3 黄金向量（control/control、view/view、requested=view+granted=control）+ granted 篡改对照 +
    C# 常量块输出。**期望值不取自被测实现**（ADR-034 纪律）。
  - 测试 +34（Security 66→100）；`docs/PROTOCOL_AND_SECURITY.md` §9 本地修订 1
    （头部「本地修订记录」+ 双档/绑 grant 修订块；该文件自此进入「随里程碑修订」模式）。
- **变异 ×4（全部精确命中，恢复后 grep/diff 确认干净）**：M1 client 档尾随 `\0` → 8 红；
  M2 cert 小写 hex → 8 红；M3 去 `server\0` 前缀 → 4 红 → **发现
  `ServerProof_DependsOnServerPrefix` 只断言常量关系、不触实现** → 强化为「实现输出 ≠
  无前缀 HMAC」→ **重放 M3 = 5 红**；M4 去掉 serverNonce 长度校验 → 3 红（theory 全 case）。
- **坑**：① C# 字符串 `\0` **后跟数字会被解析成八进制转义**（uuid 串都以数字开头！）——
  测试黄金串里 NUL 一律写 `\u0000`（Python 脚本输出时自动转换；手写 `"\0111…"` 会静默出错）；
  ② 变异验证的真实收益：M3 抓出「表面相关的测试没真正覆盖实现」——不是走形式。
- **下一站**：阶段 2 = 认证消息帧 JSON 严格解析（AuthChallenge / AuthResponse / AuthSuccess +
  ApprovalPending + canonical base64），照 `HelloFrame` 严格模式（重复/未知字段、大小写、深度全写死）。
