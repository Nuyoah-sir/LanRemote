# 2026-09-22

## M4 阶段 1 完成 —— 双档 transcript + HMAC proof 纯函数核心（提交 `35506b5`）

**feat 提交 `35506b5`**（6 files, +1113/−11）；步骤 5–8 全落地；全量 **643 PASS / 0 FAIL**。

- **产出**：
  - `src/LanRemote.Security/Auth/AuthProtocol.cs`——协议词汇表单一事实源：域串
    （`LANREMOTE-AUTH-V1` / `LANREMOTE-GRANT-V1` / `server\0`）、5 帧 type、16 字段名、
    4 尺寸（nonce/proof/token/cert 均 32B）、时限初值（认证 10s / 审批 60s，provisional）、
    权限词（view/control）+ `EncodePermission`（未定义枚举值抛异常）。
  - `src/LanRemote.Security/Auth/AuthTranscriptBuilder.cs`——纯函数核心：双档 transcript
    （ClientAuthTranscript / ServerGrantTranscript）+ `ComputeClientProof` / `ComputeServerProof`；
    输入全部强类型（Guid/Span/枚举）**不收 string**；固定长度（nonce×2/cert/key）fail fast。
  - `scripts/reference/gen-auth-golden-vectors.py`（入库）——独立 Python 参考实现（标准库），
    3 黄金向量（control/control、view/view、requested=view+granted=control）+ granted 篡改对照 +
    C# 常量块输出。**期望值不取自被测实现**（ADR-034 纪律）。
  - 测试 +34（Security 66→100）；`docs/PROTOCOL_AND_SECURITY.md` §9 本地修订 1
    （头部「本地修订记录」+ 双档/绑 grant 修订块；该文件自此进入「随里程碑修订」模式）。
- **变异 ×4（全部精确命中，恢复后 grep/diff 确认干净）**：M1 client 档尾随 `\0` → 8 红；
  M2 cert 小写 hex → 8 红；M3 去 `server\0` 前缀 → 4 红 → **发现
  `ServerProof_DependsOnServerPrefix` 只断言常量关系、不触实现** → 强化为「实现输出 ≠
  无前缀 HMAC」→ **重放 M3 = 5 红**；M4 去掉 serverNonce 长度校验 → 3 红（theory 全 case）。
- **坑**：① C# 字符串 `\0` **后跟数字会被解析成八进制转义**（uuid 串都以数字开头！）——
  测试黄金串里 NUL 一律写 `\u0000`（Python 脚本输出时自动转换；手写 `"\0111…"` 会静默出错）；
  ② 变异验证的真实收益：M3 抓出「表面相关的测试没真正覆盖实现」——不是走形式。
- **下一站**：阶段 2 = 认证消息帧 JSON 严格解析（AuthChallenge / AuthResponse / AuthSuccess +
  ApprovalPending + canonical base64），照 `HelloFrame` 严格模式（重复/未知字段、大小写、深度全写死）。

## 重定位：认证核心 Security → Transport（`e7687ec`，ADR-039）

- 阶段 2 开工盘点**实测**：Transport(net10.0) 引用 Security(net10.0-windows7.0) →
  **NU1201 构建失败**（不是判断，是构建器判决；实验后 csproj 已回退）。
- 认证核心（AuthProtocol/AuthTranscriptBuilder）+ 34 条测试 git mv 至 Transport(.Tests)，
  namespace `LanRemote.Transport.Auth`；协议字节/黄金向量零变化；全量 643 不变
  （Transport 192→226 / Security 100→66）。
- ADR-039 已立（含实测证据）；两 csproj 注释 + 协议工作副本路径同步。
- **教训**：跨 TFM 依赖方向——net10.0 项目**不能**引用 net10.0-windows 项目；
  「无 Windows API 依赖的纯逻辑」不要放 windows-TFM 项目，否则传输层无法消费。

## M4 阶段 2 完成 —— 认证消息帧（JSON 严格解析）（提交 `21a8829`）

**feat 提交 `21a8829`**（18 files, +3031）；§18.2 步骤 9–12 全落地 + 1 个计划外帧；
全量 **877 PASS / 0 FAIL**（Transport 226→460 = +234）。

- **产出**：
  - 帧类 ×5（`src/LanRemote.Transport/Auth/`）：`AuthChallengeFrame`（步骤 9）/
    `AuthResponseFrame`（步骤 10，`clientName` 硬化 ≤64/无控制字符/UTF-16 良构）/
    `AuthSuccessFrame` + `ApprovalPendingFrame`（步骤 11）/ **计划外
    `AuthenticationFailedFrame`**（阶段 3/4 公共前置件：语义为空、永不携带原因码）。
  - canonical 编码 ×3（步骤 12）：`CanonicalBase64`/`CanonicalHex`/`CanonicalGuid`——
    判定式 = **round-trip 逐字符相等**（宽松 decode → 重新 encode → Ordinal 比较）。
  - `AuthJson` 增**类型提示预读**（`expectedType`/`wrongTypeCode` 可选参数 +
    `TryReadTypeHint`）——跨帧载荷报 wrong-type 而非 malformed；主解析仍是唯一权威。
  - 合同级决定已 **ADR-040**（canonical 判定式 / 拒绝分类 / 短码只留本地 / clientName 策略）。
- **实测事实**：① `Utf8JsonReader.GetString()` 对非法 UTF-8 抛 `InvalidOperationException`
  （非 JsonException）；② 孤立代理转义 `\uD800` 直接 JsonException → malformed；
  ③ `Guid.TryParseExact("D")` 容忍大写**和前后空白**（round-trip 是唯一收窄者）；
  ④ `"AA++"`/`"AA//"` 是合法 canonical base64（先入为主误判被实测纠正）。
- **变异 ×4（全部精确命中，恢复后 grep/diff 确认干净）**：M1 重复字段放行 → 5 红；
  M2 GUID round-trip 失效 → 6 红；M3 challenge nonce 长度判定摘除 → **首轮 0 红** →
  **抓到假测试**（手抄「31 字节」串实为 45 字符 excess padding 非法 base64）→ 测试加
  `B64(int)` helper（BCL 编码器构造邻界）→ 重放 **1 红**；M4 success proof 长度判定摘除
  → **1 红**。
- **教训**：长 base64 测试字面量**一律编码器现造**，手抄必错；邻界值（31/33 字节）只有
  真构造才能测到长度判定——变异验证第二次抓到同类假测试。
- **下一站**：阶段 3 = 服务端认证状态机（challenge 生成 / 验证链 / limiter / local approval +
  `approval_pending` / sessionToken + SessionRegistry；衔接层 ADR-037 随之落地）。

## M4 阶段 3 完成 —— 服务端认证状态机（衔接层 + 验证链 + 限流 + 审批 + 会话登记）（提交 `760e950`）

**feat 提交 `760e950`**（14 files, +2780/−89）；§18.2 步骤 13–17 全落地 + ADR-037 衔接层同批；
全量 **902 PASS / 0 FAIL**（Transport 460→485 = +25）；合同级决定 **ADR-041**。

- **产出**：
  - 衔接层（ADR-037 可执行形式）：`ConnectionSecurityContext`（冻结快照：ConnectionId / 地址 /
    TLS 版本 / 证书 DER SHA-256）+ `ControlPreAuthHandoff`（exactly-once `BeginAuthentication`）+
    `AcceptedConnection` 收编为 `(Security, Stream)`；`ControlPreAuthSession` 成功路径不再关连接；
    `AllowedOperationsWhilePreAuthenticated` = 恰好一项 `begin-authentication`
    （M3 空集合门禁**有意识改写**）；`ControlSessionState` 扩展 Authenticating / Authenticated。
  - `ControlAuthSession`（664 行）八段主干：限流前置 → 机器窗口（10 s 绝对 deadline，
    覆盖写/读/校验）→ 帧级严格解析 → 密钥加载（**解析后才触碰**）→ 重算 + `FixedTimeEquals`
    （唯一计限流 + 300–800 ms 随机延时）→ 审批（quota → approval_pending → 三路竞速）→
    serverProof + token + auth_success → 登记 + 保持。成功帧序列恰为
    `[challenge, approval_pending, success]`。
  - `FailedAuthLimiter`（10 min 窗 / 5 失败 → 60 s 封；**到点放行、再失败即再封**；成功清全部；
    懒清理；零日志）；`ILocalApprovalGate`（5 值显式终态 + fail closed + 短关联码
    `SHA256("LANREMOTE-APPROVAL-CODE-V1"\0‖sessionId‖clientNonce)`）；
    `SessionRegistry`（internal Register 唯一入口 + token 防御性拷贝 + Dispose 注销清零 +
    反射测试钉死公开面）。
- **实测事实**：① **夹具拆线竞速**——harness 出结局后立即 cancel+dispose 与客户端「读终帧」抢跑
  （快速失败路径偶发丢帧）；修复 = 拆线前 `await Task.WhenAny(clientTask, Task.Delay(3s))`；
  ② 「迟到决定」延时**不绑 stall 令牌**（绑了会被 harness 取消吞掉）；③ 机器窗口压线后置校验
  （两行代码，没有它 deadline 会漂移）；④ xUnit2017：用 `Assert.DoesNotContain`。
- **变异 ×4（恢复后 grep/diff 确认干净）**：M1「proof 校验恒成功」→ **4 红精确**
  （错钥 / 篡改权限 / 篡改指纹 / 失败延时边界；提交后原样重放核实）；M2「删 RecordFailure」→
  3 红（三条计数断言）；M3「IsGrantable 恒真」→ 1 红（Over_Grant）；M4「双拆断连防线」→
  1 红（Approval_Disconnect）——**纵深防御「全拆才红」形态**（两道防线各自在场测不出）。
- **下一站**：阶段 4 = 客户端侧（步骤 18–20：认证流程（**绑 presentedPin** 的 transcript +
  验证 serverProof）/ 失败处理（UI 文案「远端身份验证失败……」+ 不发送输入）/ 客户端 e2e
  真 TLS 双端：correct key / wrong key / serverProof 篡改必拒）。
