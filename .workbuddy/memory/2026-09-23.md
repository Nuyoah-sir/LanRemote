# 2026-09-23

## M4 阶段 5 实施前外部评审材料

- 按用户“你要问你就写prompt啊”要求，完成 `outputs/m4-stage5-review/M4_STAGE5_ACCEPTANCE_REVIEW_PROMPT.md` 与配套 `M4_STAGE5_ACCEPTANCE_REVIEW_EVIDENCE.zip`。聚焦批准前短码通知、真实 WPF gate 生命周期、密钥显示/输入、会话保持及双端证据；明确 A/B 方案仍待用户批准、最小审批面与接受时刻已定不重问。
- 包内296项：201个源码/项目文件（含验收器全部20个跟踪文件）、84份前次验证证据，另含合同/说明/prompt/清单。ZIP CRC及295个被列成员SHA-256全通过；包SHA-256：`ccd09011c308453f6fa56bca949e73c2d633e5c41d7793c622f09dc847bc45b4`。不含真实密钥、PFX、DPAPI数据或二进制；未自动外发。
- 代码仍为 `bc02a0c`，打包检查点 `c3011e9`；src/tests/tools与提交无差异。只核对前次Debug/Release各1036 PASS原始日志，本轮没有重新构建测试、没有GUI/Dispatcher/两机实验、没有网络/真实密钥操作。
- 提示词经独立只读复核后纠正两处：`AuthenticatedControlSession`仅实现`IDisposable.Dispose`，没有`DisposeAsync`；前置未满足的现有日志码为`UNMET`。不能根据摘要推断不存在的API。
- 已在HANDOFF §18.10追加交接；本轮文档记账未提交/推送。下一步停在用户转发材料并贴回正式评审，回收后先核对事实，再确认两项交互并实施阶段5，不重复旧时限评审。

## 阶段 5 自主推进授权

- 用户随后明确“国外模型暂时用不了了，你现在全权继续下去吧”，前述等待点失效。不再等待外部模型或重复确认 A/B；选定验收器显式查看本机 key、遮挡且不持久化的对端输入、批准前短码通知和有界审批收件箱。安全合同、线上帧和静默改网禁令不变。
- 已说明当前尚不能看屏或键鼠控制，剩余为 M4 收尾及 M5–M10；首次视频闭环完成后重估工作量，不能按测试数量折算产品完成率。

## 阶段 5 接线、验证与交付收口

- 更正上一段时间线：“已说明”当时被提前记账；实际正式进度/工期答复在本次续接时才给出。剩余为 M4 人工收尾 + M5–M10；主屏只看约1–3周、基本控制约3–6周、自用稳定约6–12周或更久，前提为范围稳定、持续开发、真机及时配合，不是交付日期承诺。
- 阶段5主实现提交 `6c7a15b`：客户端首次合法 pending 非秘密通知，有界审批 inbox（RequestId+Generation），Host/Client 高层完整认证，WPF key/短码/审批与关窗、context 单一 WorkLease，真实 registry 保持证据及先清理后唯一结算。没有产品 App 看屏/输入能力，没有改网络/防火墙或读取真实 key。
- 复核发现原最终构建其实有8条 xUnit2031 警告；仅两测试文件等价改写后提交 `7a9d199`。新 Debug/Release 全量各1190 PASS、0 FAIL、0 SKIP，build均0警告/0错误；14–17号原始日志对应最终测试树。
- 8项变异运行期全部kill并恢复：7项直接断言；M02异常泄漏被夹具清理重抛，不能说原始断言诊断直接可见；M03/M05为复合变异，非每处独立覆盖。四目标源文件SHA与基线/最终记录一致；无编译失败冒充kill。完整范围与局限写入HANDOFF §18.11 / ADR-044。
- 最终自包含WPF认证候选包265项/60,265,589 bytes，SHA256 `4972edee30e7d213c9805c179c0e69f0f3a532fe261073e2207ab67932308f19`，程序集包含 `7a9d199`。CRC、发布目录逐字节、说明、ps1 BOM、AMD64 GUI PE均核对通过；静态检查不是人工窗口通过。
- 当前真正关口：用户协助两台同子网RFC1918 Windows机器做GUI/认证演练；180秒Host自然结束，success按SessionId、负例按四元组配对。人工GUI/DPI/两机尚未运行，M4不能标整体完成；无需再转外部模型或重问已授权的交互方案。网络准备可能断网，仍须用户明确确认和UAC。
